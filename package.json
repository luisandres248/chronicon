{
  "name": "chronicon_v5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:native": "VITE_CAPACITOR=true vite build",
    "preview": "vite preview",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync",
    "android:sync": "npm run build:native && npx cap sync android",
    "android:run": "npm run build:native && npx cap sync android && cd android && ./gradlew installDebug",
    "android:bundle": "npm run android:sync && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.0",
    "@capacitor/cli": "^7.6.0",
    "@capacitor/core": "^7.6.0",
    "date-fns": "^2.29.3",
    "i18next": "^25.3.4",
    "react": "^18.2.0",
    "react-day-picker": "^9.14.0",
    "react-dom": "^18.2.0",
    "react-i18next": "^15.6.1",
    "react-router-dom": "^7.1.5",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.1.2",
    "vite-plugin-pwa": "^1.2.0"
  }
}
